"""
Services — Business Logic Layer

This package contains business logic that doesn't belong
in CRUD (data access) or routes (API layer).

Examples:
    - Appointment conflict detection
    - Waitlist queue management & auto-assignment
    - Notification dispatch & retry logic
    - Provider availability slot calculation
    - Activity log helpers
"""
